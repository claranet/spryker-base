
server {
    listen 8080;

    server_name zed.de.spryker.dev;

    keepalive_timeout 0;
    access_log  /data/logs/zed-access.log extended;

    root /data/shop/public/Zed;

    set $application_env development;
    set $application_store DE;
    include "spryker/zed.conf";
}
